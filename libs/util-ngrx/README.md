# util-ngrx

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build util-ngrx` to build the library.

## Running unit tests

Run `nx test util-ngrx` to execute the unit tests via [Jest](https://jestjs.io).
